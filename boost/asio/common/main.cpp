#include <iostream>
#include "common.h"

using namespace std;

int main()
{
    // test
    //string fileName = "/Users/a123/Downloads/share/library/Qt/qtftp-master.zip";
    //cout << readFile(fileName) << endl;
    //cout << getFileMd5(fileName) << endl;
    //cout << getFileSize(fileName) << endl;
    //cout << getFileName(fileName) << endl;

    /*
    MessageInfo info;
    info.insert(g_file, getFileName(fileName));
    info.insert(g_size, getFileSize(fileName));
    info.insert(g_md5, getFileMd5(fileName));
    cout << info.toString() << endl;
    */

    //readFileBySize(fileName);

    cout << "hello world" << endl;
    return 0;
}
